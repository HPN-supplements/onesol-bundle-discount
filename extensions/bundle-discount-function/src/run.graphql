query Input {
  cart {
    lines {
      id
      quantity
      bundleItem: attribute(key: "_bundle_item") {
        value
      }
      bundleId: attribute(key: "_bundle_id") {
        value
      }
    }
  }
  discount {
    discountClasses
  }
}
